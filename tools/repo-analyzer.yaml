name: repo-analyzer
description: Analyze repository structure and metrics

input_schema:
  type: object
  properties:
    path:
      type: string
      description: Path to repository
  required:
    - path

# No runtime command allowed in strict v0.1.0 validation



